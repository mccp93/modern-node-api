const routers = {
  playerRouter: require('./router.player'),
  resultRouter: require('./router.result'),
  fixtureRouter: require('./router.fixture'),
  userRouter: require('./router.user'),
  postRouter: require('./router.post'),
};

module.exports = routers;
